{
  "name": "strong-deploy",
  "description": "deploy a node package using git",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/strongloop/strong-deploy.git"
  },
  "author": "engineering@strongloop.com",
  "main": "index.js",
  "scripts": {
    "test": "tap --timeout=300 ./test/test-*.js"
  },
  "bin": {
    "sl-deploy": "./bin/sl-deploy",
    "sld": "./bin/sl-deploy"
  },
  "devDependencies": {
    "tap": "^0.4.9"
  }
}
